"""
Validation / Safety Agent

Responsibility:
    Perform deterministic validation:
    - Claim amount must respect policy coverage
    - Deductible must be correctly applied
    - Claim type must match policy
    - Required output schema must be valid
    - Invalid or unsafe proposals must fail safely
    - High-impact payout actions must require human approval
"""

# TODO: Implement validation/safety agent logic
